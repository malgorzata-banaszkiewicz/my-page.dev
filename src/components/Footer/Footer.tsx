import { FooterWrapper } from './FooterWrapper';

export const Footer: React.ElementType = ({color}) => (
    
    <FooterWrapper color={color}>
        <span>😀Dziękuję za uwagę😀</span>
    </FooterWrapper>
)